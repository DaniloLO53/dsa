// Given an array of integers nums and an integer k, return the number of contiguous
// sub-arrays where the product of all elements in the sub-array is strictly less than k.

import mergeArr from '../sort/mergeSort.js'

function foo(arr, k) {
    const arrs = []

    for (let i = 0; i < arr.length; i++) {

    }
}

// [2,5,3,1,3,4] -> order -> [1,2,3,3,4,5]

//  i
// [1,2,3,3,4,5]
//            j


const arr = [2,5,3,1]
console.log(foo(arr))